# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json
type: files

files:
  # Copy wallpapers to KDE's wallpaper directory
  - source: usr/share/wallpapers/rigel-os
    destination: /usr/share/wallpapers/rigel-os
